SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__encounter_performed_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113883.3.117.1.7.1.424" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) AS NonElectiveEncounter WHERE ((DATEDIFF(NonElectiveEncounter.relevantPeriod.high, NonElectiveEncounter.relevantPeriod.low) <= 120) AND NonElectiveEncounter.relevantPeriod.high BETWEEN _parameters.measurementPeriod.low AND _parameters.measurementPeriod.high))) AS NonElectiveEncounter WHERE EXISTS (SELECT * FROM (SELECT * FROM (NonElectiveEncounter.diagnoses)) AS Diagnosis WHERE ((Diagnosis.rank = 1) AND (EXISTS (SELECT codes.code AS code FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113883.3.117.1.7.1.212" ORDER BY version DESC LIMIT 1) WHERE code = Diagnosis.code) OR EXISTS (SELECT codes.code AS code FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113883.3.117.1.7.1.247" ORDER BY version DESC LIMIT 1) WHERE code = Diagnosis.code)))))) AS AllStrokeEncounter WHERE (DATEDIFF(year, (SELECT birthDatetime FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'common__patient') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep) AS SingletonFrom WHERE SingletonFrom.patientId = AllStrokeEncounter.patientId LIMIT 1)), AllStrokeEncounter.relevantPeriod.low) >= 18))) AS EncounterWithAge WHERE EXISTS (SELECT * FROM (SELECT * FROM (EncounterWithAge.diagnoses)) AS Diagnosis WHERE (EXISTS (SELECT codes.code AS code FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113883.3.117.1.7.1.247" ORDER BY version DESC LIMIT 1) WHERE code = Diagnosis.code) AND (Diagnosis.rank = 1)))) AS _source SEMI JOIN SELECT * FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__intervention_order_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "1.3.6.1.4.1.33895.1.3.0.45" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) FULL JOIN (SELECT * FROM ( SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__intervention_performed_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "1.3.6.1.4.1.33895.1.3.0.45" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) ON 1=0 AS ComfortMeasure ON _source.cannotPrintIdFor_ = ComfortMeasure.cannotPrintIdFor_ WHERE coalesce(IF (NOT ComfortMeasure.relevantDatetime AS  SUCH THAT )THEN (SELECT struct(ComfortMeasure.relevantDatetime as high, ComfortMeasure.relevantDatetime as low) _val) ELSE (IF (NOT ComfortMeasure.relevantPeriod AS  SUCH THAT )THEN (ComfortMeasure.relevantPeriod) ELSE (NULL)).low, ComfortMeasure.authorDatetime) BETWEEN LET last_value(SELECT * FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__encounter_performed_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113762.1.4.1111.143" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) AS LastObs WHERE (LastObs.relevantPeriod.high BETWEEN SELECT struct(Visit.relevantPeriod.low as high, (Visit.relevantPeriod.low - INTERVAL 1 HOUR) as low) _val.low AND SELECT struct(Visit.relevantPeriod.low as high, (Visit.relevantPeriod.low - INTERVAL 1 HOUR) as low) _val.high AND NOT Visit.relevantPeriod.low AS  SUCH THAT ) ORDER BY relevantPeriod.high ASC) AS ObsVisit, coalesce(ObsVisit.relevantPeriod.low, Visit.relevantPeriod.low) AS VisitStart, last_value(SELECT * FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__encounter_performed_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113883.3.117.1.7.1.292" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) AS LastED WHERE (LastED.relevantPeriod.high BETWEEN SELECT struct(VisitStart as high, (VisitStart - INTERVAL 1 HOUR) as low) _val.low AND SELECT struct(VisitStart as high, (VisitStart - INTERVAL 1 HOUR) as low) _val.high AND NOT VisitStart AS  SUCH THAT ) ORDER BY relevantPeriod.high ASC) AS EDVisit SELECT * FROM (SELECT SELECT struct(Visit.relevantPeriod.high as high, coalesce(EDVisit.relevantPeriod.low, VisitStart) as low) _val FROM ()) AS Visit.low AND LET last_value(SELECT * FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__encounter_performed_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113762.1.4.1111.143" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) AS LastObs WHERE (LastObs.relevantPeriod.high BETWEEN SELECT struct(Visit.relevantPeriod.low as high, (Visit.relevantPeriod.low - INTERVAL 1 HOUR) as low) _val.low AND SELECT struct(Visit.relevantPeriod.low as high, (Visit.relevantPeriod.low - INTERVAL 1 HOUR) as low) _val.high AND NOT Visit.relevantPeriod.low AS  SUCH THAT ) ORDER BY relevantPeriod.high ASC) AS ObsVisit, coalesce(ObsVisit.relevantPeriod.low, Visit.relevantPeriod.low) AS VisitStart, last_value(SELECT * FROM (SELECT * FROM (SELECT _dataType.*, GETDATE() _evaluatedOn, DATE_TRUNC('MM', _ep.measurementPeriod.high) _partitionKey, STRUCT(STRING(_ep.measurementPeriod) key, _ep.measurementPeriod measurementPeriod) _parameters FROM {{ source('fhirlake', 'qdm__encounter_performed_v56') }} _dataType CROSS JOIN {{ ref('system__evaluation_period') }} _ep WHERE EXISTS (SELECT * FROM (SELECT explode(codes) codes FROM (SELECT codes FROM (WITH cy2024 AS (SELECT _c1 `oid`, _c0 `name`, _c2 `version`, _c8 `code`, _c9 `display`, _c10 `codeSystem`, _c11 `codeSystemOid`FROM CSV.`/mnt/zones/subenv/{{ env_var('workgroup_subenv') }}/specification/valueset/vsac/ec_eh_oqr_unique_vs_20230504.csv.gz`), all AS (SELECT t.oid, t.name, t.version, STRUCT(t.code, t.display, cs.url system) code FROM cy2024 t LEFT JOIN {{ source('fhirlake', 'terminology__codesystem') }} cs ON CONCAT('urn:oid:', t.codeSystemOid) = cs.identifier[0].value) SELECT oid, FIRST(name) name, version, COLLECT_LIST(code) codes FROM all GROUP BY oid, version) WHERE oid = "2.16.840.1.113883.3.117.1.7.1.292" ORDER BY version DESC LIMIT 1)) AS _vs WHERE _dataType.code.code = _vs.codes.code AND _dataType.code.system = _vs.codes.system))) AS LastED WHERE (LastED.relevantPeriod.high BETWEEN SELECT struct(VisitStart as high, (VisitStart - INTERVAL 1 HOUR) as low) _val.low AND SELECT struct(VisitStart as high, (VisitStart - INTERVAL 1 HOUR) as low) _val.high AND NOT VisitStart AS  SUCH THAT ) ORDER BY relevantPeriod.high ASC) AS EDVisit SELECT * FROM (SELECT SELECT struct(Visit.relevantPeriod.high as high, coalesce(EDVisit.relevantPeriod.low, VisitStart) as low) _val FROM ()) AS Visit.high)) AS IschemicStrokeEncounter